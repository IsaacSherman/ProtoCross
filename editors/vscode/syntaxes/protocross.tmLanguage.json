{
  "$schema": "https://raw.githubusercontent.com/martinring/tmlanguage/master/tmlanguage.json",
  "name": "ProtoCross",
  "scopeName": "source.pcross",
  "comment": "Colours a ProtoCross file before the language server answers, and wherever it never will: a diff, a preview, a machine with the server turned off. Every scope a token the server classifies can land in is the TextMate scope VS Code maps that classification to, qualified only by the language, so the server's colours replace these with the same colours rather than flickering to new ones. The server's test suite sweeps every ProtoCross source in the repository to hold that. Keywords are spec 6.4's list; identifiers are coloured as the server's lexical layer colours them, as variables, and the server refines them.",
  "patterns": [
    { "include": "#comments" },
    { "include": "#strings" },
    { "include": "#numbers" },
    { "include": "#keywords" },
    { "include": "#identifiers" },
    { "include": "#operators" },
    { "include": "#punctuation" }
  ],
  "repository": {
    "comments": {
      "patterns": [
        {
          "comment": "Runs to the end of the line and does not take the newline.",
          "name": "comment.pcross",
          "match": "//.*"
        },
        {
          "comment": "Does not nest: the first */ closes it, as the lexer decides (spec 6.2). Unterminated, it runs to the end of the file.",
          "name": "comment.pcross",
          "begin": "/\\*",
          "end": "\\*/"
        }
      ]
    },
    "strings": {
      "patterns": [
        {
          "comment": "Ends at the closing quote or at the end of the line (spec 6.1). Escapes are consumed so an escaped quote does not close it, and are not given a scope of their own: the server colours the whole literal as one string.",
          "name": "string.pcross",
          "begin": "\"",
          "end": "\"|$",
          "patterns": [
            { "match": "\\\\." }
          ]
        }
      ]
    },
    "numbers": {
      "patterns": [
        {
          "comment": "A '.' begins a fractional part only when a digit follows it; there is no exponent syntax (spec 6.1).",
          "name": "constant.numeric.pcross",
          "match": "\\p{Nd}+(?:\\.\\p{Nd}+)?"
        }
      ]
    },
    "keywords": {
      "patterns": [
        {
          "name": "keyword.control.pcross",
          "match": "(?<![\\p{L}\\p{Nd}_])(?:and|arg|as|bool|break|bytes|case|continue|double|else|enum|expect|extend|fail|false|float|fn|for|has|if|import|in|int32|int64|message|not|on_zero|or|proto|receiver|return|string|switch|test|true|uint32|uint64|var|void|while)(?![\\p{L}\\p{Nd}_])"
        }
      ]
    },
    "identifiers": {
      "patterns": [
        {
          "name": "variable.other.readwrite.pcross",
          "match": "[\\p{L}_][\\p{L}\\p{Nd}_]*"
        }
      ]
    },
    "operators": {
      "patterns": [
        {
          "name": "keyword.operator.pcross",
          "match": "->|==|!=|<=|>=|&&|\\|\\||[-+*/%=!<>]"
        }
      ]
    },
    "punctuation": {
      "patterns": [
        { "name": "punctuation.terminator.pcross", "match": ";" },
        { "name": "punctuation.separator.pcross", "match": "," },
        { "name": "punctuation.accessor.pcross", "match": "\\." }
      ]
    }
  }
}
